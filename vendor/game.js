// Generated by CoffeeScript 1.9.3
(function() {
  var Game;

  Game = (function() {
    function Game(newNumber) {
      this.newNumber = newNumber;
    }

    Game.prototype.getNumber = function() {
      return this.newNumber;
    };

    Game.prototype.isGameOver = function() {
      return this.gameOver;
    };

    Game.prototype.guessCount = 0;
    Game.prototype.gameOver = false;

    Game.prototype.getGuessesCount = function() {
      return this.guessCount;
    };

    Game.prototype.aGuess = function(guessingNumber) {
      if (!guessingNumber) return;
      if (!this.gameOver) {
        this.guessCount += 1;
      }
      if (this.newNumber < guessingNumber) {
        return "The number is smaller";
      } else if (this.newNumber > guessingNumber) {
        return "The number is bigger";
      } else {
        this.gameOver = true;
        return "You guessed right";
      }
    };

    return Game;

  })();

  define("Game", [], function() {
    return Game;
  });

}).call(this);
